export const enum NODE_ENVS {
  production = 'production',
}

export const enum DEV_MODE {
  server = 'server',
}

export const enum categories {
  code = 'code',
  map = 'map',
  note = 'note',
  canvas = 'canvas',
  gym = 'gym',
  extend = 'extend',
  japnese = 'japanese',
  english = 'english',
}
